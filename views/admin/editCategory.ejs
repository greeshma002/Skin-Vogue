<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 20px;
        }

        .edit-container {
            max-width: 400px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin-bottom: 8px;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #ff69b4;
            color: #fff;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #d8509b;
        }
    </style>
</head>
<body>
    <div class="edit-container">
        <h2>Edit Category</h2>
        <form id="editCategoryForm" action="/admin/categories/edit/<%= category._id %>" method="post" onsubmit="return validateCategoryName()">
            <label for="categoryName">Category Name:</label>
            <input type="text" id="categoryName" name="categoryName" value="<%= category.categoryName %>" required>
            
            <span id="categoryNameError" style="color: red;"></span>
    
            <button type="submit">Save</button>
        </form>

        
    
        <script>
            function validateCategoryName() {
                var categoryNameInput = document.getElementById('categoryName');
                var categoryNameError = document.getElementById('categoryNameError');
                var categoryName = categoryNameInput.value;
    
                
                if (/[\d\s~`!@#$%^&*()_+={}\[\]:;<>,.?/"'\\|[\]\s]/.test(categoryName)) {
                    categoryNameError.innerText = 'Category name should not contain numbers, special characters, or spaces.';
                    return false;
                } else {
                    categoryNameError.innerText = '';
                    return true;
                }
            }
        </script>
    </div>
    
</body>
</html>
