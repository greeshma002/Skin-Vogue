<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Address Management System</title>
    <style>
        body {
            font-family: poppins;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        header {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 10px;
        }

        h1 {
            margin: 0;
            padding: 20px 0;
            color: #333;
        }

        .address-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            margin: 20px;
        }

        .address-box {
            width: 45%;
            margin-bottom: 20px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            box-sizing: border-box;
        }

        .add-btn {
            display: block;
            margin: 20px auto;
            padding: 10px 20px;
            background-color: #ff69b4;
            color: white;
            text-decoration: none;
            text-align: center;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s ease-in-out;
        }

        .add-btn:hover {
            background-color: #d9534f;
        }

        .edit-btn,
        .delete-btn {
            display: inline-block;
            margin-top: 10px;
            padding: 8px 12px;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
        }

        .edit-btn {
            background-color: #5bc0de;
        }

        .delete-btn {
            background-color: #d9534f;
        }

        .edit-btn:hover,
        .delete-btn:hover {
            filter: brightness(90%);
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            background-color: #ff69b4;
            color: #fff;
        }

        .brand {
            font-weight: bold;
            display: flex;
            align-items: center;
        }

        .brand img {
            margin-right: 10px;
            height: 30px;
        }

        .navbar-links {
            display: flex;
            align-items: center;
        }
    </style>
</head>
<body>
    
    <div class="navbar">
        <div class="brand">
            <img src="/assets/img/logo.png" alt="Logo">
            <div><strong>SKINVOGUE</strong></div>
        </div>
        <div class="navbar-links">
            <a href="/" class="link">Home</a>
            
        </div>
    </div>

    <a class="add-btn" href="#">Manage Addresses</a>
  
    <div class="address-container">
      <% addresses.forEach(address => { %>
        <div class="address-box">
          <h3><%= address.name %></h3>
          
          <p>Phone: <%= address.phone %></p>
          <p>Address: <%= address.Address %>, <br> <%= address.city %>, <%= address.state %>, <%= address.pin %></p>
          
          <a href="/address/<%=address.id %>">
            <button class="edit-btn" type="button" onclick="editAddress('<%= address.id %>')">Edit Address</button>
          </a>
          
          <form method="get" action="/deleteAddress/<%=address.id%>" onsubmit="return confirm('Are you sure you want to delete this address?')">
            <button type="submit" class="delete-btn">Delete</button>
          </form>
        </div>
      <% }); %>
    </div>
  
</body>
</html>
