<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Address Management System</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 20px;
            background-color: #f4f4f4;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .address-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .address-box {
            width: 100%;
            margin-bottom: 20px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
            box-sizing: border-box;
        }

        .add-btn {
            display: block;
            margin: 20px 0;
            padding: 10px;
            background-color: #ff69b4;
            color: white;
            text-decoration: none;
            text-align: center;
            border-radius: 5px;
        }

        .add-btn:hover {
            background-color: #ff69b4;
        }

        .form-container {
            max-width: 400px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #333;
        }

        input[type="text"], input[type="email"], input[type="tel"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            box-sizing: border-box;
        }

        input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        input[type="submit"]:hover {
            background-color: #45a049;
        }
    </style>
</head>
<!-- manage-addresses.ejs -->
<body>

    <a class="add-btn">Manage Addresses</a>
  
    <div class="address-container">
      <% addresses.forEach(address => { %>
        <div class="address-box">
          <h3><%= address.name %></h3>
          
          <p>Phone: <%= address.phone %></p>
          <p>Address: <%= address.Address %>, <br> <%= address.city %>, <%= address.state %>, <%= address.pin %></p>
          <a href="/address/<%=address.id %>">  
        <button class="_32l7f0" type="button" onclick="editAddress('<%= address.id %>')">Edit address</button></a>
          
          <form method="get"  action="/deleteAddress/<%=address.id%>" onsubmit="return confirm('Are you sure you want to delete this address?')">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
          

          


        </div>
      <% }); %>
    </div>
  
  </body>
  
</html>
